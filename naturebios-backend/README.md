# Nature Biosciences 官网管理系统

## 📋 项目概述

本系统是一套完整的生物科研试剂企业官网+后台管理系统，包含：

- **前端网站**：产品展示、搜索、文献墙
- **后台管理**：产品管理、用户权限、批量操作
- **数据库**：MySQL，支持100,000+产品

---

## 🚀 快速开始

### 1. 环境要求

| 软件 | 版本 |
|------|------|
| Node.js | ≥ 18 |
| MySQL | ≥ 8.0 |
| pnpm | ≥ 8.0 |

### 2. 安装步骤

#### 后端安装

```bash
cd naturebios-backend
cp .env.example .env
# 编辑 .env 配置数据库连接
npm install
npm start
```

#### 前端安装

```bash
cd naturebios-frontend
npm install
npm run dev
```

### 3. 访问地址

| 服务 | 地址 |
|------|------|
| 前端网站 | http://localhost:5173 |
| 后端API | http://localhost:3000 |
| 管理后台 | http://localhost:5173/login |

### 4. 默认账户

```
管理员：admin / admin123
```

---

## 📖 功能说明

### 🔍 搜索算法

系统实现了**智能相关度排序**：

1. **SKU精确匹配** → 最高优先级
2. **基因符号匹配** → 高优先级
3. **产品名称匹配** → 中优先级
4. **描述内容匹配** → 基础优先级

热门产品、文献引用多的产品会获得额外加分。

### 📝 产品模板（5种）

| 模板 | 适用产品类型 | 特点 |
|------|-------------|------|
| A | 普通抗体 | 完整信息展示 |
| B | 试剂盒 | 套装组成展示 |
| C | 生化试剂 | 分子信息 |
| D | 内参对照 | 内参方案推荐 |
| E | 磷酸化抗体 | 修饰位点强调 |

**模板可在后台切换，实时生效**

### 👥 权限系统

| 角色 | 产品管理 | 用户管理 | 系统设置 |
|------|---------|---------|---------|
| 1级管理员(admin) | ✅ 全部 | ✅ 全部 | ✅ 全部 |
| 2级分管理员(sub_admin) | ✅ 本人创建 | ❌ | ❌ |

### 📦 批量操作

- **批量导入**：支持Excel导入，智能识别字段
- **批量修改**：价格、库存、状态一键更新
- **批量删除**：多选删除
- **模板下载**：提供导入模板

---

## 📊 数据库结构

### 产品表 (products)

```sql
-- 核心字段
id: UUID (主键)
sku: VARCHAR(50) (唯一)
name: VARCHAR(255)
category: ENUM (6种分类)
templateType: ENUM (A-E模板)
price: DECIMAL
stock: INT
status: ENUM (published/draft/archived)

-- 详细信息 (30+字段)
geneSymbol, uniprotId, host, clonality
applications (JSON), species (JSON), reactivity (JSON)
modification, phosphorylationSite...
```

### 用户表 (users)

```sql
id: UUID (主键)
username: VARCHAR(50) (唯一)
password: VARCHAR(255)
role: ENUM (admin/sub_admin)
name, email, phone, status...
```

---

## 🔧 常用命令

```bash
# 后端
npm start          # 启动API服务
npm run dev        # 开发模式

# 前端
npm run dev        # 开发服务器
npm run build      # 生产构建
```

---

## 📁 项目结构

```
naturebios-backend/
├── src/
│   ├── config/       # 数据库配置
│   ├── models/       # 数据模型
│   ├── routes/       # API路由
│   ├── services/     # 业务逻辑
│   ├── middleware/   # 权限中间件
│   └── index.js      # 入口文件
├── uploads/          # 上传文件目录
└── package.json

naturebios-frontend/
├── src/
│   ├── components/   # UI组件
│   ├── pages/        # 页面
│   ├── services/     # API调用
│   ├── types/        # TypeScript类型
│   └── App.tsx       # 主应用
└── package.json
```

---

## ⚠️ 注意事项

1. **首次启动**：系统会自动创建admin账户
2. **数据库**：需要提前创建空数据库 `naturebios`
3. **图片**：产品图片存放在 `uploads/` 目录
4. **备份**：`backup/` 目录用于数据备份

---

## 📞 技术支持

- 电话：13376277882
- 邮箱：tech@naturebios.com

---

*Version 1.0 | Generated: 2026-02-26*
